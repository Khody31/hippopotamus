#pragma once

#include <QRect>

namespace menu_constants {
  // game menu
const QRect resume_button = QRect(35, 20, 30, 10);
const QRect restart_button = QRect(35, 35, 30, 10);
const QRect sound_button = QRect(35, 50, 30, 10);
const QRect to_main_menu_button = QRect(35, 65, 30, 10);
  // main menu
const QRect start_game_button = QRect(35, 20, 30, 10);
const QRect settings_button = QRect(35, 35, 30,10);
const QRect exit_button = QRect(35, 50, 30, 10);
  // settings
const QRect sound = QRect(35, 20, 30, 10);
const QRect to_main_menu = QRect(35, 35, 30, 10);

}  // menu_constants